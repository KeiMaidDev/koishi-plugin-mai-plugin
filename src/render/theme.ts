const colors = Object.freeze({
  background: '#f7f8fa',
  surface: '#ffffff',
  text: '#20242c',
  mutedText: '#697386',
  accent: '#00a8a8',
  highlight: '#f4b942',
})

export const MAIMAI_RENDER_THEME = Object.freeze({
  fontFamily: 'Noto Sans SC',
  colors,
})
